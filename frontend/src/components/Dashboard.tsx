import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div>
            <h1>Benvenuto nella Dashboard</h1>
        </div>
    );
};

export default Dashboard; // Esporta il componente come default
